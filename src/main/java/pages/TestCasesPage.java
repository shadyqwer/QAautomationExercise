package pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.ui.WebDriverWait;

public class TestCasesPage extends IndexPage {
    public TestCasesPage(WebDriver driver, WebDriverWait wait) {
        super(driver, wait);
    }
}
